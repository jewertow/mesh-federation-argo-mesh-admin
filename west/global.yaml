global:
  configNamespace: istio-system
  local:
    export:
    - hostname: httpbin-service.mesh.global
      ports:
      - 8000
      - 9000
  remote:
  - mesh: east-mesh
    addresses:
    - console-openshift-console.apps.east.servicemesh.devcluster.openshift.com
    port: 443
    network: east-network
    locality: us-east1

